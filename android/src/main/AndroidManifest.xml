<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mirrorfly.mirrorfly_plugin">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/><!--for use full screen intent-->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" /><!--for redmi device-->

    <!-- Request legacy Bluetooth permissions on older devices. -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <!-- Needed only if your app communicates with already-paired Bluetooth
        devices. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" /><!--AudioManager#startBluetoothSco() requires MODIFY_AUDIO_SETTINGS permission.-->

    <application>
        <activity
            android:name=".call.CallKitUiActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:resizeableActivity="false"
            android:screenOrientation="portrait"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:supportsPictureInPicture="true"
            android:taskAffinity="call.video"
            android:turnScreenOn="true"
            android:windowSoftInputMode="stateHidden"
            android:lockTaskMode="if_whitelisted"
            tools:targetApi="R"
            android:theme="@style/CallkitIncomingTheme"
            android:exported="false" />
        <activity
            android:exported="false"
            android:theme="@style/TranslucentTheme"
            android:name=".call.TransparentActivity"/>
    </application>

</manifest>